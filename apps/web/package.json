{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next export -o out",
    "build:static": "next build && next export -o out",
    "start": "next start"
  },
  "dependencies": {
    // твои зависимости (оставь как есть)
  }
}
